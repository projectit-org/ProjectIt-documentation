// Generated by the ProjectIt Language Generator.
import { observable, makeObservable } from "mobx";
import { model, MobxModelElementImpl, observablelistpart, observablepart, PiNamedElement, PiUtils } from "@projectit/core";
import { Type, AttributeWithLimitedType, AttributeWithEntityType, EntityFunction, EntityMetaType, PiElementReference } from "./internal";

/**
 * Class Entity is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to changes in the state of its properties.
 */
@model
export class Entity extends MobxModelElementImpl implements PiNamedElement, Type {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data
     */
    static create(data: Partial<Entity>): Entity {
        const result = new Entity();
        if (!!data.isCompany) {
            result.isCompany = data.isCompany;
        }
        if (!!data.simpleprop) {
            result.simpleprop = data.simpleprop;
        }
        if (!!data.name) {
            result.name = data.name;
        }
        if (!!data.baseInterface_attr) {
            result.baseInterface_attr = data.baseInterface_attr;
        }
        if (!!data.attributes) {
            data.attributes.forEach(x => result.attributes.push(x));
        }
        if (!!data.entAttributes) {
            data.entAttributes.forEach(x => result.entAttributes.push(x));
        }
        if (!!data.functions) {
            data.functions.forEach(x => result.functions.push(x));
        }
        if (!!data.int_attrs) {
            data.int_attrs.forEach(x => result.int_attrs.push(x));
        }
        if (!!data.int_functions) {
            data.int_functions.forEach(x => result.int_functions.push(x));
        }
        if (!!data.baseEntity) {
            result.baseEntity = data.baseEntity;
        }
        if (!!data.baseInterface) {
            result.baseInterface = data.baseInterface;
        }
        return result;
    }

    readonly $typename: EntityMetaType = "Entity"; // holds the metatype in the form of a string
    $id: string; // a unique identifier
    isCompany: boolean = false; // implementation of isCompany
    simpleprop: string = ""; // implementation of simpleprop
    name: string = ""; // implementation of name
    baseInterface_attr: number = 0; // implementation of baseInterface_attr
    attributes: AttributeWithLimitedType[]; // implementation of part 'attributes'
    entAttributes: AttributeWithEntityType[]; // implementation of part 'entAttributes'
    functions: EntityFunction[]; // implementation of part 'functions'
    int_attrs: AttributeWithLimitedType[]; // implementation of part 'int_attrs'
    int_functions: EntityFunction[]; // implementation of part 'int_functions'
    baseEntity: PiElementReference<Entity>; // implementation of reference 'baseEntity'
    baseInterface: PiElementReference<Entity>; // implementation of reference 'baseInterface'

    constructor(id?: string) {
        super();
        if (!!id) {
            this.$id = id;
        } else {
            this.$id = PiUtils.ID(); // uuid.v4();
        }
        // both 'observablepart' and 'observablelistpart' change the get and set of an attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable
        observablelistpart(this, "attributes");
        observablelistpart(this, "entAttributes");
        observablelistpart(this, "functions");
        observablelistpart(this, "int_attrs");
        observablelistpart(this, "int_functions");
        observablepart(this, "baseEntity");
        observablepart(this, "baseInterface");
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    piLanguageConcept(): EntityMetaType {
        return this.$typename;
    }

    /**
     * Returns the unique identifier of this instance.
     */
    piId(): string {
        return this.$id;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    piIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    piIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    piIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    piIsBinaryExpression(): boolean {
        return false;
    }
}
